# Data-Quant 实施状态

本文只记录仓库可验证的事实，不替代已批准的重构计划。某项能力只有在源码、契约、测试和验收命令同时给出证据时才标记为已落地。

## 当前验收基线

- `bash scripts/full_check.sh`：Bundle/已提交 Schema/来源镜像、offline-only 静态边界、核心 Ruff、64 脚本关键错误 lint、Mypy、Pytest 与 full legacy/example smoke 全部通过；当前为 **63 个核心源码文件、232 个测试**。
- `PATH="$PWD/.venv/bin:$PATH" bash statistical-learning-analysis/scripts/smoke_check.sh --full`：**64 个公共遗留脚本**索引、全部 CLI help、三条示例流水线和 gate 输出契约通过。
- `tests/e2e/`：`quantctl run -> verify-artifact -> report`、原生/兼容能力目录及 data→research→validation→portfolio→execution→risk→monitoring→governance→report 的原生 Artifact/gate；另有五资产同一 Manifest 成功矩阵、期权静态套利 blocker 与加密 maintenance-margin blocker 失败路径。

## 已有可执行证据

| 领域 | 已落地证据 |
| --- | --- |
| 统一路由与决策契约 | `SKILL.md`、9 个 active 子 Skill、`references/decision-ontology.md`、`references/workflow-contract.md` |
| Artifact / Run Record / Manifest | `src/data_quant/contracts/`、`schemas/`、Schema 导出与一致性检查；`DiagnosticSpec` 绑定阶段和 canonical inputs；三十二项原生诊断发布 strict Pydantic 参数模型、CLI/committed JSON Schema，未知键、缺失必填项、范围或 stage 错误在创建运行目录前 fail closed；`quantctl verify-artifact` 要求 content digest 与 artifact identity 同时精确匹配。 |
| Python 核心与 CLI | `src/data_quant/`、`quantctl`、`scripts/full_check.sh`；checked-in Manifest 相对输入路径已有 CLI 验收，相对输出写入外部 `DATA_QUANT_RUN_ROOT`/XDG state；`quantctl list-capabilities --json` 暴露 native stage/parameter Schema；runtime-only wheel 与完整 Skill Bundle 的边界、能力可用性和独立版本规则由 `references/distribution.md` 固化 |
| 数据/PIT/日历/标签 | canonical table contracts、数据适配器、PIT join、calendar、return labels 及单元/性质/集成测试；timestamp 字段拒绝无显式时区输入，calendar-date 与 timestamp 语义分离；`calendar.sessions_source` 必须绑定已声明的 sessions 表并校验 calendar ID、IANA timezone 与 session 窗口；43 项 canonical tables 含 effective-dated borrow availability；return labels 强制显式 type、gross/excess basis、corporate-action policy、currency，并要求 excess benchmark |
| 遗留兼容 | 64 项固定 catalog、`quantctl diagnostic` 适配器、索引检查、兼容与全量 smoke 验收；通用 CLI 保留调用者工作目录的相对路径语义并接受显式 `--format json`；完整 Bundle 中标记 `available=true`，runtime-only wheel 中如实降级为 `source_bundle_required` |
| 金融验证与 ML | purged/embargo split、factor IC Artifact、fold-local tabular baseline；`purged-walk-forward`、split/cash-dividend raw-vs-total-return-adjusted adjacent-close reconciliation 与 fixed-income irregular-stub/accrued clean-dirty price reconciliation、PIT instrument spread-node/zero-curve no-extrapolation discounting 与 floating-coupon/scheduled-amortization balance reconciliation 与 ex-coupon negative-accrued adjustment 已接入 validation Artifact/handoff/gate |
| 组合/回测/执行/风险/监控 | 优化器与成本、原生离线组合回测及 Artifact、current/target weights→lot/min-notional/participation 约束订单、离线成交回放与核算（market/limit、GTC/DAY/IOC、expiry、partial fills、arrival/VWAP shortfall）、协方差/波动率、historical ES + explicit scenario stress、PSI drift、signal freshness/coverage/dispersion/recent-IC health；`factor-ic` research、`portfolio-eligibility`/`short-borrow-capacity`/`portfolio-backtest` portfolio、`execution-replay`/`rebalance-replay`/`futures-roll-execution` offline execution、`covariance-risk`/`portfolio-stress`/`factor-risk`/`factor-attribution`/`credit-migration-stress` risk 与 `feature-drift`/`signal-health`/`model-calibration`/`service-health` monitoring 已由 Manifest 写出阶段 Artifact/handoff/gate；backtest 直接 API 强制完整 execution/return 窗口、单一 simple/gross/total-return label、非重叠区间和币种一致，并计入 flat fallback 或 bounded PIT cash/financing curve interpolation 及 flat short-borrow rates；report 从同一 Run Record 自动渲染；有 evidence gap 时 gate 为 `conditional_pass`、action 为 `hold`，并禁止将执行结果晋级 `paper_trading`。 |
| 新资产类别 | 五个 active Skill、canonical contracts 与基础 API；Manifest 原生 `futures-roll`/`futures-roll-execution`、`option-surface-check`/`option-surface-smooth`/`option-hedge-replay`、`fixed-income-shock`/`fixed-income-curve-stress`（PIT spread nodes、zero-curve no-extrapolation）/`credit-migration-stress`、`fx-rollover`/`fx-forward-check`、`crypto-margin-stress`/`crypto-cross-margin-stress` 写出版本化 Artifact/gate，覆盖 expiry/confirmed-volume/confirmed-OI 换月、同刻 gap-adjusted futures return、flat collateral attribution、exact bid/ask roll close/open、fees、end-of-day variation margin cash、PIT margin/price-limit + collateral-haircut gates、IV/静态套利、latest-common PIT multi-expiry/log-moneyness smoothing + bounded term interpolation、dynamic European IV/Greeks + discrete delta-hedge PnL/cost、利率平移/曲线冲击、PIT rating migration/spread-duration/default-recovery loss、CIP rollover、joint-calendar value dates、forward bid/ask 与 explicit cross-currency basis、isolated funding/maintenance 与 effective-tier cross-margin、insurance/socialized-ADL、venue-default recovery stress、volume-participation order-book liquidation impact 及失败关闭测试 |
| 来源/CI/文档 | `source-registry.yaml` 是 authoring path/mirror target/effective date/access timestamp/content digest 的权威源；同步脚本校验可用 authoring tree，缺失时仍校验 frozen mirror 摘要；五资产已有 effective-dated official normalized source cards，区分 retrieved/metadata-only，绑定 HTTPS publisher metadata、response/card SHA-256、bounded fresh-check claims，并拒绝 tamper/metadata drift；父仓库 CI 先验证陈旧 Schema，再执行 offline-only guard、关键遗留 lint、clean-room wheel build/install/core smoke，并在 Python 3.14 跑 full legacy smoke |
| 实盘与凭据安全边界 | Bundle/Router/Artifact 均声明 offline only；运行时代码不含下单或资金划转能力；Manifest 拒绝 URI userinfo/敏感查询参数、递归自由配置敏感键及 Diagnostic 敏感参数；`execution` 只允许 offline replay/paper simulation，live/broker/下单/划转/凭据存储配置均 schema-invalid，校验错误隐藏原始输入 |

## 尚未完成的计划项

1. **回测与组合深层语义**：原生组合回测已 fail closed 校验完整时点/标签窗口、重叠、simple/gross/total-return、币种、成本与 NAV，并计入 flat cash/financing/short-borrow rates；`portfolio-eligibility` 已审计 PIT dynamic universe、公司行动窗口与 borrow flag。`rebalance-replay` 已将同一时点 current/target weights 转换为受 lot/minimum-notional/quote-participation 约束的离线订单并回放。split 与 cash-dividend 的 raw/total-return-adjusted 相邻收盘回报 reconciliation 已有正式 blocker；bounded PIT cash/financing curves 已按 decision-time availability/effective interval 与实际持有期做无外推线性 interpolation；公司行动相邻收盘对账已覆盖 split/conversion/stock-merger、cash-dividend（flat withholding）、spin-off、cash merger、rights 与 fractional cash-in-lieu；late vendor revision 已按 available_at 版本选择并可 fail closed；PIT locate snapshots 已按 historical midpoint/static portfolio value 换算 shares，并 gate horizon-stable remaining quantity、buffer、expiry、known recall 与 cheapest-allocation blended fee；回测/组合深层语义的 named 缺口（FIFO tax lots、decision-time nav、sells-before-buys、cross-decision signed-quantity netting、secured financing blend、locate reuse/unscheduled-recall/lender concentration）已落地；仍保持 diagnostic-only，不实盘下单。
2. **风险/执行/监控扩展**：purged validation、covariance/volatility contribution、historical ES/显式情景压力、market/limit + expiry/IOC replay、participation/成交质量、PSI 与信号 freshness/coverage/dispersion/recent-IC blocker 已有原生 Artifact/gate；PIT factor exposure/return attribution、factor/specific exact reconciliation 与 gross/factor/specific formal limits 已落地；decision-time PIT exposure + complete non-overlapping factor-return sample covariance、annualized factor volatility/component decomposition、condition/exposure/component limits 已落地；PIT probability calibration 的 Brier/log-loss/ECE/linear reliability 与阈值 blockers 已落地；required-service freshness/window coverage/uptime/traffic/error/p95-latency/status blockers 已落地；非线性借记已用 leverage² convexity 加价落地（另有 linear/square-root temporary impact、permanent participation impact、queue_priority、Ledoit-Wolf/regime/specific-risk covariance、bootstrap ECE、dependency-health）。隐藏流动性与限价 amend（amended_at/amend_limit_price）已落地。
3. **资产类别深层能力**：五资产已有版本化 Artifact 与同一 Manifest 验收；期货 expiry/volume/OI 迁移、roll-gap return adjustment、exact bid/ask roll execution、fees、end-of-day variation-margin cash timing、PIT margin/price-limit 与 collateral haircut 已落地；期货日内追保已用 session high/low 对 prior settle 做最差变动保证金检查（PIT position limits、delivery、forced flatten、collateral FX、concentration、roll depth/impact 已落地），期权单合约 dynamic IV/Greeks 与 discrete delta-hedge PnL/cost 已落地；latest-common PIT multi-expiry/log-moneyness rolling-median smoothing 与 bounded term interpolation 已落地；期权三次总方差、受限 SVI、五参数 raw SVI 与 power-law SSVI（θ(T) 单调 + 共用 ρ/η）已落地；固收 regular dated schedule/session adjustment、PIT zero nodes、DV01/key-rate scenarios 与 explicit irregular accrual/coupon, accrued/clean/dirty per-100-face reconciliation 已落地；固收 Bermudan 已按付息日 backward induction，并可选用两状态利率树（rate_volatility）；另有 projection floating、brentq OAS、PIT spread nodes；PIT transition matrix、spread-duration migration、default probability stress/recovery 已落地（stochastic recovery、second-order spread/convexity、one-factor Gaussian migration-correlation tail、rating-liquidity premium 与 realized-default settlement 已落地）；FX joint-calendar spot/forward value dates、outright/points bid-ask、explicit basis/CIP deviation 与 PIT replacement-cost settlement-fail exposure 已落地；FX settlement-fail credit 已用 notional×p×LGD 限额落地（另有 CLS cutoff/allowlist、nostro/funding cap、broken-date 与 rate bid/ask）。加密 effective tiered cross-margin、funding、maintenance liquidation、insurance/socialized-ADL waterfall 与 venue-default recovery 已落地；加密多印记日内强平已按 `intraday_path` 在各 print 之间做 sequential 减仓，并记录每个 print 后的 `remaining_quantities`（另有 cumulative tiers、collateral haircut/FX、venue ADL ranking）；当前 evidence gap 收敛为 bankruptcy-price rules 与 recovery timing。
4. **来源治理扩展**：两个本地书稿镜像、runtime lock 与五资产官方 normalized source cards 已有必填 accessed_at/effective date、retrieved-vs-metadata-only、response/card digest、bounded fresh-check claim 与 tamper/metadata-drift gate；venue-product-rules 与 vendor-data-dictionary 的 metadata-only official snapshots、`source-rule-freshness` 新鲜度/权威性 gate，以及 offline `source_change_approvals` 变更审批台账已落地；本地文件来源解析强制限制在 Bundle root 内；定期网络采集仍不自动执行（offline-only）。
5. **最终验收矩阵与 CI 可见性**：五资产 baseline 已有同一 Manifest 冻结成功矩阵与期权/加密失败路径；`source-rule-freshness` 与 corporate-action late-revision 已冻结 Manifest 成功/失败路径；stochastic Bermudan Manifest 路径已冻结；父仓库 `.github/workflows/data-quant.yml` 已声明 Python 3.11/3.14 矩阵并执行 Bundle、offline-only、Schema、Mypy、Pytest、wheel 与 64-script smoke；本地隔离 Python 3.11 已按同一核心步骤通过 231 tests 和 full legacy smoke；本地 Python 3.14 完整验收通过 232 tests。远端 GitHub 运行仍需将 `data-quant/` 纳入版本控制后才会产生；遗留脚本已应用 96 条 Ruff 安全修复、格式化 66 个文件、清零 F401/F841/B007、E731 与 B905，并清理 `threshold_tuning.py`、`sklearn_tabular_model.py`、`anomaly_score_report.py`、`classification_report.py`、`cluster_quality_report.py`、`cross_sectional_return_regression.py`、`bootstrap_reality_check.py`、`calibration_report.py`、`capacity_impact_report.py`、`data_freshness_report.py`、`ewma_volatility.py`、`event_study_report.py`、`alpha_research_gate_report.py`、`covariance_report.py`、`causal_balance_check.py`、`execution_slippage_report.py`、`fama_macbeth_regression.py`、`factor_exposure_regression.py` 长行；完整规则剩余 142 条 E501 长行迁移债务。

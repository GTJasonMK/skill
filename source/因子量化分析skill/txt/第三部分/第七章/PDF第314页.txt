  特征因子调整法的核心是自助法（bootstrap）。该方法将样本因子协方差矩阵
 视为“总体”，并以它作为数据生成过程（data generating process）的已知参数，
通过模拟（˜simulation）计算出该参数下的“样本”协方差矩阵，记为
         分别求出对˜应的特征因子协方差矩阵D和 。由自助法的性质可
知， 和D之间的偏差近似等于D和真实但未知的特征因子协方差矩阵之间的偏
差。使用该偏差来调整D并进而计算 就可以得到真实协方差矩阵Σλ的更好的估
计。下面来看具体步骤。

  （1）对因子样本协方差矩阵 进行特征分解得到特征向量矩阵U和对角矩
阵D。

  （2）令D（k）表示D对角线上的第k个元素。对于特征因子组合k，假设其收
益率满足正态分布N（0, D（k））。在第m次模拟中，使用该分布生成全部T期样
本，作为特征因子k的收益率序列。对全部K个特征因子进行上述操作得到K×T收
益率矩阵，记为bm。使用bm和U计算出因子收益率矩阵 ：




  式中下标m表示第m次模拟。

  （˜3）计算因子收益率 的“样本”协方差矩阵          ：




  （4）对   进行特征分解，得到对应的特征向量矩阵               和对角阵   。

  （5）由于“真实”的协方差矩阵 已知，利用它和             求出“真实”的特征因子协
方差矩阵，记为Dm：




   （6）将上述（2）到（5）步重复M次。在每次模拟中，对给定的特征因
子k，比较Dm和 对角线上第k个元素的差异，取全部M次模拟差异的均值，按照

式（7.42）计算平均偏差:

  式中Dm（k）和             对角线上第k个元素。

    （7）在模拟中假设收益率满足正态分布以及平稳性。然而在真实世界中，实
际的收益率序列无法很好地满足上述两点。为此，需要在偏差式（7.42）的基础
上对其进行额外的缩放，以应对经验波动率偏误。缩放后，特征因子k的偏差
νs（k）为：

             νs（k）=a（ν（k）-1）+1   （7.43）
